<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.whitedesert.photosign">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application

        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"

        >

        <activity
            android:name="net.whitedesert.photosign.activities.DrawSignActivity"
            android:parentActivityName="net.whitedesert.photosign.activities.SignaturesActivity"

            />

        <activity
            android:name="net.whitedesert.photosign.activities.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />


                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.GalleryActivity"
            android:parentActivityName=".activities.MainActivity">
            <intent-filter>
                <category android:name="android.intent.category.APP_GALLERY" />
            </intent-filter>

        </activity>

        <activity
            android:name=".activities.SigningActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name="com.luminous.pick.MainActivity"

            android:label="@string/gallery_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.luminous.pick.CustomGalleryActivity">
            <intent-filter>
                <action android:name="luminous.ACTION_PICK" />
                <action android:name="luminous.ACTION_MULTIPLE_PICK" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.TextSignActivity"
            android:parentActivityName=".activities.SignaturesActivity" />

        <activity
            android:name=".activities.SignaturesActivity"
            android:parentActivityName=".activities.MainActivity" />
    </application>

</manifest>
